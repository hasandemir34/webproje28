@model IEnumerable<stokprojesi1.Models.Category>

@{
    ViewData["Title"] = "Kategoriler";
}

<div class="container mt-4">
    <h2>📦 Kategori Listesi</h2>
    <p>
        <a asp-action="Create" class="btn btn-primary">Yeni Kategori Ekle</a>
    </p>

    <table class="table table-striped table-bordered">
        <thead>
            <tr>
                <th>Kategori Adı</th>
                <th>İşlemler</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model) {
                <tr>
                    <td>@item.Name</td>
                    <td>
                        <a asp-action="Delete" asp-route-id="@item.CategoryId" class="btn btn-danger btn-sm" onclick="return confirm('Silmek istediğine emin misin aga?');">Sil</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>